---
export interface Props {
 post: any;
}
const { post } = Astro.props;

const article_text = "text-[15px] font-normal leading-[130%]";
---

<article id="article" class="p-[20px] rounded-[10px] bg-[#FBFBFF]">
 <div class="flex items-center justify-between">
  <time datetime={post.releaseDate} class={article_text}>
   ⌚ {
    new Date(post.releaseDate).toLocaleDateString("ru-RU", {
     year: "numeric",
     month: "numeric",
     day: "numeric",
    })
   }
  </time>
  <!-- <span class={article_text}>{post.author}</span> -->
 </div>
 <div class="flex flex-col my-[30px]">
  <h3 id="line-clamp" class="text-[20px] font-medium leading-[120%]">{post.title}</h3>
  <p id="line-clamp" class={"mt-[10px] " + `${article_text}`}>{post.excerpt}</p>
 </div>
 <div class="flex items-center justify-between">
  <a
   href={"/guide/" + post.slug.current}
   class="bg-[#D6E1FF] hover:bg-[#aec0f1] rounded-[10px] py-[4px] px-[14px] text-[15px]">👉 Подробнее</a
  >
  <!-- <span class={article_text}>📖 5 мин.</span> -->
  <!-- <span>{post.categories}</span> -->
 </div>
</article>

<style>
 #article:nth-child(2n) {
  background: #fbfffc;
 }
 #article:nth-child(2n) a{
  background: #D6FFDF;
 }
  #article:nth-child(2n) a:hover{
  background: #b1f2bf;
 }
 #line-clamp {
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2; /* number of lines to show */
  line-clamp: 2;
  -webkit-box-orient: vertical;
 }

</style>
